# Copy this file to .env before starting the app.

# Core app routing
NEXT_PUBLIC_APP_URL="http://localhost:3000"
DEFAULT_LOCALE="en"
SUPPORTED_LOCALES="en"
NEXT_PUBLIC_WHATSAPP_ADVERT_NUMBER="+256783230321"

# Database
DATABASE_URL="mysql://root:password@localhost:3306/equip_blog"

# Session and secrets
SESSION_SECRET="replace-with-a-long-random-secret"
SESSION_MAX_AGE_SECONDS="28800"
AI_PROVIDER_CONFIG_SECRET="replace-with-a-second-long-random-secret"
REVALIDATE_SECRET="replace-with-a-revalidation-secret"
CRON_SECRET="replace-with-a-cron-secret"

# AI provider bootstrap defaults used by prisma seed
AI_PROVIDER_DEFAULT="openai"
AI_MODEL_DEFAULT="gpt-5.4"
AI_PROVIDER_FALLBACK="openai"
AI_MODEL_FALLBACK="gpt-5.4-mini"
OPENAI_API_KEY="replace-with-your-openai-key"

# Media storage
# Use local uploads for development, or switch MEDIA_DRIVER to s3 and fill in the S3 values.
MEDIA_DRIVER="local"
LOCAL_MEDIA_BASE_PATH="public/uploads"
LOCAL_MEDIA_BASE_URL="/uploads"
S3_MEDIA_BUCKET="your-bucket"
S3_MEDIA_REGION="your-region"
S3_MEDIA_BASE_URL="https://cdn.example.com"
S3_ACCESS_KEY_ID="your-access-key"
S3_SECRET_ACCESS_KEY="your-secret-key"

# Seeded admin account
# These are bootstrap defaults only. Change them in .env before production use.
ADMIN_SEED_EMAIL="admin@admin.com"
ADMIN_SEED_PASSWORD="admin"

# Public comments
COMMENT_RATE_LIMIT_WINDOW_MS="60000"
COMMENT_RATE_LIMIT_MAX="5"
COMMENT_CAPTCHA_ENABLED="false"
COMMENT_CAPTCHA_SECRET=""

# Upload policy
UPLOAD_ALLOWED_MIME_TYPES="image/jpeg,image/png,image/webp"

# cPanel deployment helper
# These values are only used by the local cPanel automation scripts.
CPANEL_SSH_HOST="example.com"
CPANEL_SSH_PORT="22"
CPANEL_SSH_USER="cpanel-user"
CPANEL_SSH_KEY_PATH=""
CPANEL_APP_PATH="/home/cpanel-user/nodeapps/equip-blog"
CPANEL_GIT_BRANCH="main"
CPANEL_ALLOW_DATA_RESET="false"
