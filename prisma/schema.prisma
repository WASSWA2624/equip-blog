generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model ScaffoldMarker {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  label     String   @default("repo-scaffold")
}
